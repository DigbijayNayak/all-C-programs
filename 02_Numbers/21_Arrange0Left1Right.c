// Arrange Zeros to left side and Ones right side in Arrays
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int i, size, j, zerosCount = 0;
    printf("Enter Size of Array: \n");
    scanf("%d", &size);
    int a[size];
    printf("Enter 1's and 0's only: \n");
}